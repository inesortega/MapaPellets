version: '3'
services:

    pellets-shiny:
        restart: always
        image: pellets:latest
        build: .
        container_name: pellets-shiny
        volumes:
          - ./:/app
        ports:
          - 3838:3838/tcp
        networks:
          - default

networks:
  default:
